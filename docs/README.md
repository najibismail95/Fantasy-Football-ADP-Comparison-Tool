# docs/

Images referenced by the top-level README.

- `actions-summary.png` — the daily report as it renders on a workflow run's
  Summary page in GitHub Actions. Capture it signed in, since GitHub hides run
  summaries from logged-out visitors.
